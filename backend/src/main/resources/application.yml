spring:
  application:
    name: backend
  profiles:
    active: mariadb,filler
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
  data:
    rest:
      base-path: /api
      detection-strategy: annotated
    mongodb:
      username: root
      password: imse24
      database: topic_exchange
      authentication-database: admin
      host: localhost
      port: 27017
  datasource:
    url: jdbc:mariadb://127.0.0.1:3306/topic_exchange
    username: root
    password: imse24
  jackson:
    default-property-inclusion: non_null
