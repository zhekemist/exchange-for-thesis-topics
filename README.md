# Topic Exchange for Bachelorthesis

## Running the Project

1. Build the backend and the containers

```bash
docker compose build
```

2. Run the containers

```
docker compose up -d
```

3. The website should now be reachable under the URL `https://localhost`
